{
    "version": 2,
    "buildCommand": "npm run vercel-build",
    "installCommand": "npm install --no-optional",
    "outputDirectory": "dist",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}